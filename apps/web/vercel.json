{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && bun install --frozen-lockfile",
  "buildCommand": "cd ../.. && bun run --filter @ronin-dojo/web build",
  "crons": [
    {
      "path": "/api/cron/publish-tools",
      "schedule": "0 0 * * *"
    }
  ]
}
