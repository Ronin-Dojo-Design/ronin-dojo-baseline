# Dirstarter upstream provenance
#
# This app started as a copy of the Dirstarter (Polarsoft) template.
# The inner .git was stripped on copy so this monorepo has one git history.
# If you want to pull future Dirstarter updates, clone the upstream into
# a sibling worktree and cherry-pick or three-way-merge changes by hand.

upstream_url    = https://github.com/dirstarter/dirstarter.git
upstream_branch = main
copied_at_sha   = c42e8bbc9a093daa8bb70faebfc552399134ee13
copied_on       = 2026-04-25
copied_from     = /Users/brianscott/Local Sites/DirStarter /dirstarter_template

# Partial upstream ports:
# - SESSION_0205 / L2: env/deploy-safe subset from upstream 7e724b6 applied.
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0206 / L3: schema wave subset from upstream 7e724b6 applied
#   (ToolTier enum/tierPriority, archived statuses, Bookmark, report enum,
#   post/bookmark slices, slug helpers).
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0207 / L4: listing tier flow and public relabel subset from
#   upstream 7e724b6 applied on Ronin's existing Tool routes/model
#   (public tier badges, bookmark affordance, admin tier transition audit).
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0208 / L5: UI primitives Part 1 from upstream 7e724b6 applied —
#   components/common/{field,button-group,tool-status}.tsx ported; tools-table
#   refactored to consume toolStatusBadgeProps / toolStatusIcon. Data-table
#   helpers (faceted-filter, view-options) already matched upstream behavior;
#   the render={} vs asChild Popover API drift is deferred to a later
#   Radix-API lane.
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0209 / L6 Phase 1: Base UI migration foundation + 2 safe leaf
#   primitives applied from upstream 7e724b6 —
#   added deps @base-ui/react ^1.3.0, cmdk-base ^1.0.0, tailwind-variants ^3.2.2;
#   ported lib/slot.ts (Base UI mergeProps-based asChild util);
#   reconciled components/common/toaster.tsx (next-themes + CSS-variable styling);
#   relocated components/common/empty-list.tsx from components/web/empty-list.tsx
#   (10 import sites repathed); migrated components/common/separator.tsx and
#   components/common/avatar.tsx from radix-ui to @base-ui/react. Phases 2-8
#   (lib/utils cva→tv, Slot-only primitives, tooltip, accordion+hover-card,
#   form primitives, popover family, command+tabs+admin Cmd+K+dep cleanup)
#   tracked under drift register D-016 + petey-plan-0083.
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0210 / L6 Phase 2a: utils + AnimatedContainer + cva import sweep
#   applied from upstream 7e724b6 —
#   lib/utils.ts rewritten as tailwind-variants re-export (tv as cva, cn as cx,
#   VariantProps; unused compose/defineConfig/extendTailwindMerge dropped);
#   popoverAnimationClasses kept in Radix-state shape (Phase 7 swaps to
#   Base UI data-open/data-closed semantics when the 4 still-Radix popover-
#   family primitives migrate); components/common/animated-container.tsx
#   migrated from radix-ui Slot.Root to slot() from ~/lib/slot (12 consumer
#   sites unchanged); stray `import { cx } from "cva"` consumers
#   (components/web/ads/ads-picker.tsx, components/admin/sidebar.tsx)
#   repathed onto ~/lib/utils. Phase 2 was re-split at bow-in into 2a (this
#   session) + 2b (Heading, SESSION_0211) + 2c (Box, SESSION_0212); upstream
#   Box deletes the Box component (59 JSX + 14 internal consumers to refactor)
#   and upstream Heading adopts useRender (140 <Hn as=…> rewrites). Phases
#   3-8 slide to SESSION_0213-0218. D-016 + petey-plan-0083 updated.
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0211 / L6 Phase 2b: Heading migrated from radix-ui Slot/as/asChild
#   polymorphism to upstream @base-ui/react/use-render render={...} API. Exact
#   AST residual check found 211 Heading JSX tags and 60 IntroTitle wrapper
#   tags with zero remaining Heading/IntroTitle as/asChild props; 61 direct
#   Heading render callbacks plus 1 IntroTitle render callback preserve the
#   prior rendered element while keeping visual size props unchanged.
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0212 / L6 Phase 2c: Box migrated to upstream boxVariants-only API.
#   Removed the legacy Box component and BoxProps export; exact AST residual
#   check found 0 remaining <Box> JSX tags and 0 Box/BoxProps imports. Current
#   consumers apply boxVariants directly to real elements so the prior Slot.Root
#   DOM shape and focus/hover behavior are preserved without a Radix dependency.
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0214 / L6 Phase 5: HoverCard and Accordion migrated to upstream
#   Base UI runtime from upstream 7e724b6. components/common/hover-card.tsx now
#   wraps @base-ui/react PreviewCard Root/Trigger/Portal/Positioner/Popup while
#   preserving Ronin export names; components/web/tools/tool-hover-card.tsx uses
#   Trigger render instead of Radix asChild. components/common/accordion.tsx now
#   wraps @base-ui/react Accordion Root/Item/Header/Trigger/Panel, preserving
#   AccordionContent as the export over Panel and using data-open/data-closed
#   selectors. Tooltip Phase 4 remains pending because it is a larger consumer
#   rewrite.
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
# - SESSION_0215 / L6 Phase 4: Tooltip migrated to upstream Base UI runtime
#   from upstream 7e724b6. components/common/tooltip.tsx now wraps
#   @base-ui/react Tooltip Provider/Root/Trigger/Portal/Positioner/Popup/Arrow
#   while preserving Ronin import paths. The legacy Ronin-only `tooltip=` wrapper
#   API was removed; all current consumers now compose Tooltip + TooltipTrigger
#   render + TooltipContent. Provider `delayDuration` props were renamed to
#   Base UI `delay`.
#   copied_at_sha stays unchanged until SESSION_0218 / L15 full reconciliation.
#
# SESSION_0216 partial-port note (Phase 6 — form primitives):
#   checkbox.tsx, radio-group.tsx, switch.tsx migrated from radix-ui to
#   @base-ui/react equivalents. label.tsx dropped radix-ui LabelPrimitive.Root
#   for plain <label> with preserved Ronin labelVariants + isRequired.
#   4 DataTable select-all consumers updated: checked="indeterminate" →
#   separate indeterminate={bool} prop. field.tsx and button-group.tsx confirmed
#   non-Radix; no changes needed.
#
# SESSION_0217 (2026-05-21) — D-016 Phase 7 partial port:
#   dialog.tsx, popover.tsx, dropdown-menu.tsx, select.tsx, drawer.tsx migrated
#   from radix-ui to @base-ui/react equivalents. popoverAnimationClasses updated
#   to Base UI semantics (data-open/data-closed, --transform-origin). ~55
#   consumer asChild → render={} call sites swept. Select onValueChange type
#   signatures fixed (value as string). Drawer preserved as Ronin-custom
#   bottom-sheet on Base UI Dialog (no upstream equivalent).
