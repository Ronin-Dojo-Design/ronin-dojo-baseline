{
  "name": "@ronin-dojo/ui-kit",
  "version": "0.0.1",
  "private": true,
  "description": "Brand-agnostic shared component kernel (ADR 0033). AdminKanban (PWCC-007), m-card, design tokens — consumed by the BBL app and client CRMs by swapping a token block + a config/DTO slice.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./kanban": "./src/kanban/index.ts",
    "./m-card": "./src/m-card/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "oxlint --fix .",
    "lint:check": "oxlint ."
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  }
}
