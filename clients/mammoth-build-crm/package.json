{
  "name": "mammoth-build-crm",
  "version": "0.1.0",
  "private": true,
  "description": "Mammoth Build CRM — MVP (Next.js + Prisma, own DB per ADR 0038). Landing + projects pipeline + job order form + build-photo documentation.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate",
    "db:studio": "prisma studio",
    "db:push": "prisma db push --skip-generate",
    "db:reset": "prisma migrate reset"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "@ronin-dojo/ui-kit": "file:../../packages/ui-kit",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "prisma": "^7.8.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.6.0"
  }
}
